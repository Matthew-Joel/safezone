import React from "react";

const Headings = ({ title }: any) => {
	return (
		<div className="sm:text-5xl md:text-4xl text-4xl">
			<h2 className="">{title}</h2>
		</div>
	);
};

export default Headings;
